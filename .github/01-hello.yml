name: 01 - Hello

on:
  push:
    branches: [main]
  
jobs:
    hello:
      runs-on:  ubuntu-24.04
      steps:
          - name: Hello
            run: echo "Hello, World!"
          - name: Ver fecha del runner
            run: date
          - name: Listar archivos
            run: ls -la